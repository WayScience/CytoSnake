name: cytominer_env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python>=3.7.0
  # - conda-forge::pycytominer=0.2
  - pyyaml
  - pip
  - pip:
      - git+https://github.com/cytomining/pycytominer.git
